Always run lint and typecheck after code changes.
Only use pnpm as the package manager.
Before making UI changes, make sure similar UI doesn't exist that we can repurpose. If something doesn't exist, always default to using Shad/cn and components first.
Where possible, update product documentation and readmes if a change warrants it.
